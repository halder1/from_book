requested_topping = 'mushrooms' #Notably, it used a single = operand, because that would be setting it, 
	    						#not comparing it (as it would with ==)
if requested_topping != 'anchovies':
	print("Hold the anchovies!")s
                              